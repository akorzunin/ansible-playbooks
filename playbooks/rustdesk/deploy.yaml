- name: Deploy RustDesk server
  hosts: all
  become: false
  vars:
    deploy_compose_service_name: rustdesk

  tasks:
    - name: Docker compose up {{ deploy_compose_service_name }}
      ansible.builtin.import_role:
        name: deploy_compose
