services:
  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    network_mode: host
    # ports:
    #   - "80:80"
    #   - "443:443"
    volumes:
      - ./caddy_data:/data:rw
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
